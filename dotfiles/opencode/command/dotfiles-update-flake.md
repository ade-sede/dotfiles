______________________________________________________________________

## description: Update flake inputs (nixpkgs, home-manager, plasma-manager, nix-flatpak)

Run the `dotfiles-update-flake` skill to update all flake inputs and resolve conflicts.
