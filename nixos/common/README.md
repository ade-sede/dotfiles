# nixos/common

NixOS system configuration shared across all NixOS hosts (koala-devbox and remote-devbox).

Covers the Nix daemon, locale, timezone, user definition, printing, Home Manager NixOS module options, and Docker virtualisation. Host-specific system concerns belong in `hosts/<name>/nixos/`.
