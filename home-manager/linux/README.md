# home-manager/linux

Home Manager configuration shared across all Linux hosts (koala-devbox and remote-devbox), but not macOS.

Contains Linux-only packages, desktop GUI packages (Plasma/Wayland, imported only by koala-devbox), and the GPG agent configuration for Linux desktop use. macOS-specific concerns belong in the relevant `hosts/<name>/home-manager/` directory.
