# hosts/koala-devbox/home-manager

Host-specific Home Manager configuration for koala-devbox.

Extends the common and Linux Home Manager modules with desktop GUI packages, KDE Plasma settings (via plasma-manager), and packages only needed on this machine. The `plasma-config.nix` file is machine-generated and should not be hand-edited.
