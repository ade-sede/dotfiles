# koala-devbox

NixOS desktop machine (x86_64-linux) running KDE Plasma 6 on Wayland with SDDM.

This is the primary personal desktop. It includes a full GUI stack, Bluetooth audio, Flatpak support, and KDE-specific Home Manager configuration managed via plasma-manager.
