# hosts/alan-macbook/home-manager

Host-specific Home Manager configuration for alan-macbook.

The `default.nix` here also serves as the standalone Home Manager entry point (there is no separate `standalone.nix`). Extends the common modules with macOS GPG config and the alan-bin activation script.
