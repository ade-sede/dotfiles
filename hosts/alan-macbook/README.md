# alan-macbook

macOS machine (aarch64-darwin) — work laptop managed with standalone Home Manager only (no NixOS).

Uses the macOS system GPG pinentry dialog and a custom activation that symlinks nix binaries into `~/.alan/bin` for compatibility with corporate tooling that expects binaries in that path.
